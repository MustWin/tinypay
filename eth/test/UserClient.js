contract('UserClient', function(accounts) {

  it("Should move pricePerHit from sender to userClientMicropay", function() {
    assert.fail(1, 2, "write a test");
  });

  it("Should only RegisterHit for the created user", function() {
    assert.fail(1, 2, "write a test");
  });

});
